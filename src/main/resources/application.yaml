spring:
  profiles:
    group:
      "local": "local"
      "dev": "dev"
      "prod": "prod"
  servlet:
    multipart:
      maxFileSize: 100MB
      maxRequestSize: 200MB
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100